div(class="jumbotron")
  include appInfo

  h2(onclick='$("#hiddenDesc").show()') Description
  div(style="display: none", id="hiddenDesc")
    each note in notes
      tt=note
      br

  include appVariants
